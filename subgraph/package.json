{
  "name": "@stakefit/subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy stakefit",
    "create-local": "graph create --node http://localhost:8020/ stakefit",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 stakefit"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
